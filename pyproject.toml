[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-job"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "beaker-gantry",
]

[tool.setuptools]
py-modules = []

[tool.gantry]
